# NPM configuration for context-mcp

# Use exact versions for dependencies
save-exact=true

# Don't create package-lock.json
package-lock=false

# Registry
registry=https://registry.npmjs.org/

# Publishing configuration
access=public
